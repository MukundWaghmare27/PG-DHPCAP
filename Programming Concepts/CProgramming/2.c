#include <stdio.h>

int main(){
    int No1, No2, add, sub, mult, div;
    No1= 13;
    No2= 19;
    add = No1 + No2;
    sub = No1 - No2;
    mult = No1 + No2;
    div = No1 / No2;
    printf("Addition: %d\n", add);
    printf("Subtraction: %d\n", sub);
    printf("Multiplication: %d\n", mult);
    printf("Division: %d\n", div);
    return 0;   
}